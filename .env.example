# Copy to .env and fill in. Vite only exposes vars prefixed with VITE_.

# URL of the endpoint that receives contact-form submissions and emails them via Resend.
# Leave empty during design; the form will capture input but not send.
# e.g. https://pathway-contact.onrender.com/api/contact
VITE_CONTACT_ENDPOINT=
